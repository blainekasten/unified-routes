import React from "react"
import styled from "styled-components"

const Specs = styled.p``

const SpecHeader = styled.h1`
  font-size: 8px;
  margin: 0;
`

export default p => (
  <>
    <SpecHeader>Specs:</SpecHeader>
    <Specs>{p.data.product.specs}</Specs>
  </>
)
